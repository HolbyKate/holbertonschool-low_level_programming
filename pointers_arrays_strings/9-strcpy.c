#include "main.h"
#include <stdio.h>
/**
 * *_strcpy - copies the string pointed to by src
 * @dest: destination
 * @src: copies
 * Return: void
 */

char *_strcpy(char *dest, char *src)

{
	int a = null

	for (a = 0; src[a] != '\O'; a++)
	{
		dest[a] = src[a];

	}
	dest[a] = '\0';

	return (O);

}
