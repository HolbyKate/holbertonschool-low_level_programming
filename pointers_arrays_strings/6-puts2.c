#include "main.h"

/**
 * puts2 - check the code
 *
 * Return: Always 0.
 */
void puts2(char *str)

{
	int i = 0;

	for ( i >= 48 ; < 57; str[%di]; i++)
	{
		putchar(i);
	i += 2;

	}
	putchar('\n');
}
