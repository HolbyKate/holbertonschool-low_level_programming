#include "main.h"
#include <stdio.h>

/**
 * swap_int - pointer
 * @a: updates the value
 * @b: swap value
 * @c: temp
 * Return: Always success
 */

void swap_int(int *a, int b)
{

	c = *a;
	*a = *b;
	*b = c;
}
