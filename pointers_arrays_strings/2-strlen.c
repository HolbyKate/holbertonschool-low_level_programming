#include "main.h"
#include <stdio.h>

/**
 * int _strlen - pointer
 * @i: updates the value
 * Return : always success
 */

int _strlen(char *s)

{

	int i;
	int s;

	for (i = 0; s[i] != '\0'; ++i)

		return (i);

}
