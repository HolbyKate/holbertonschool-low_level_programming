#include "main.h"
#include <stdio.h>

/**
 * main - prints the name file, followed by a new line
 * @argc: the number of argurment
 * @argv: the array
 *
 * Return: name file
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
