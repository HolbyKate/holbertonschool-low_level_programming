#include <stdio.h>

/**
 * main - prints the name file, followed by a new line
 * @argc: the size
 * @argv: the array
 *
 * Return: name file
 */

int main(int argc__attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
