#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - function that prints a string
 * @s: string whith recursion
 *
 * Return : Always 0.
 */
void _puts_recursion(char *s)

{
	int c = 0;

	    if (s[c] == '\0');

	putchar ('\n');

	return(0);
}
