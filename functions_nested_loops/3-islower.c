#include "main.h"

/**
 * _islower - check in lowercase
 * @c: Returns 1 if c is lowercase
 * Return: Always 0.
 */

int _islower(int c)
{


	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}

			return (0);
	}
