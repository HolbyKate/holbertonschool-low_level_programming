#include "main.h"

/**
 * _abs - abs
 * @i: The standard library provides a similar function: abs.
 * Return: -i if <0 i if else
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}

